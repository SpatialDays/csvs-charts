apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geoserverdata
spec:
  storageClassName: {{ .Values.pv.local.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi