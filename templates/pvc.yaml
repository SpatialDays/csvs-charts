apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.pv.name }}
  namespace: {{ .Values.pv.namespace }}
spec:
  storageClassName: {{ .Values.pv.storageClassName }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
